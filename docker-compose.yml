version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: patent-intel-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

  redis:
    image: valkey/valkey:latest
    container_name: patent-intel-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  mongodb_data:
  redis_data:
